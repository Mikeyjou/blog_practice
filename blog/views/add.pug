extends layout

block content
    .container
        h1 新增文章
        form (action="/adding", method="POST")
            div.form-group
                label(for="title") 文章標題
                input#title.form-control(type="text",name="title")
            div.form-group
                label(for="title") 作者
                input#author.form-control(type="text",name="author")
            div.form-group
                label(for="title") 內文
                textarea#content.form-control(name="content",row=10)
            button.btn.btn-primary 送出